# Run records and per-project harness state (config.toml is yours to commit if you want it).
.lh-harness/
workspace/
runs/
logs/
results/
tmp/

# Node
node_modules/
sdk/frontend/web/dist/

# Container auth -- tokens live here at run time, never committed.
docker/.env

# Generated MCP configs can embed secrets.
*.mcp.json
