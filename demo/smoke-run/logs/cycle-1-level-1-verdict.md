PASS

I opened and independently re-ran every artifact this level names.

**Criterion 1 (≥5, ≤8 entries with a complete §1.1 header + grepped `dated_locator`):** `sources/` holds exactly 8 entry files (`sqlite-whentouse`, `sqlite-wal`, `sqlite-lts`, `sqlite-cli`, `sqlite-changes`, `duckdb-sigmod19`, `duckdb-cidr20`, `duckdb-pvldb-hashjoin`), each carrying all eleven header fields with real values, including a `dated_locator` naming the exact printed line (e.g. `sqlite-wal.md` "final line of the capture: 'This page was last updated on 2026-04-13 10:54:51Z'"). I confirmed `sqlite-changes`'s `dated: 2026-07-24` is genuinely the newest release heading — line 28 of `sources/raw/sqlite-changes.txt` reads `2026-07-24 (3.53.4)`. The two lower-precision dates (`duckdb-pvldb-hashjoin: 2025`, conference first-days for the SIGMOD/CIDR papers) are disclosed in `CLAIM.md` §1 before I found them, and each is the date the document itself prints.

**Criteria 3 & 4 (quote substance and grep verification):** I extracted all 24 `> ` quote lines directly out of the eight `sources/<key>.md` files and grepped each against its own capture with the canonical `tr -s '[:space:]' ' ' | grep -F -c` form — all 24 returned `1`, with word counts 13–50 (no quote under 12 or over 75), all body prose with locators. I then re-ran **all 32** `tr -s` one-liners in `evidence/level-1/quote-check.txt` unmodified from the repo root; every one returned `1`, matching the recorded output exactly — no contradicted transcript. Zero grep exemptions are claimed and none are needed. I also verified the PDF page locators per-page with `pdftotext -f/-l`: `duckdb-cidr20` Q1→p2, Q2/Q3→p4; `duckdb-sigmod19` Q1→p1, Q2/Q3→p3; `duckdb-pvldb-hashjoin` Q1→p1, Q2→p2, Q3→p3 — all exactly as the entries state.

**Criterion 5 (`.src` + `.txt` + `.headers.txt`, hashes, no stubs):** all 24 files exist and are non-empty; `head -c` shows the three PDFs beginning `%PDF-1.5` and the five HTML captures beginning `<!DOCTYPE html><html><head><meta name="viewport"...`, i.e. fetched bytes, not composed prose. Each `.headers.txt` carries `Date`, `Content-Type`, and `Content-Length`/`Last-Modified`/`ETag`, and the `content-length` values (7930, 21223, 41826, 93803, 473582, 197233, 974371, 1756396) match the `.src` byte sizes on disk exactly. My own `shasum -a 256 sources/raw/*.txt sources/raw/*.src` reproduced all 24 hashes in `quote-check.txt` and in `CAPTURE_LOG.md` character for character.

**Criterion 6 (capture log with exact commands):** `sources/CAPTURE_LOG.md` records per capture the URL, UTC timestamp, exact `curl -sS -L -D ... -o ...` command, status, byte size, both sha256s, and the extraction command. I re-derived three of the eight myself (`pandoc` on `sqlite-lts.src` and `sqlite-whentouse.src`, `pdftotext` on `duckdb-cidr20.src`) and each produced a file whose sha256 equals the logged `.txt` — Traceability 5's re-derivation test passes. §3a–f log the cidrdb.org connection failure, the 404, the 569-byte `/docs/stable/` JS redirect stub, and the `duckdb.org` header deficiency with decisions taken.

**Criterion 7 (excluded sources):** `claims/excluded-sources.md` lists four rejected documents, all four appearing as real fetches in `CAPTURE_LOG.md` §2 with captures under `sources/rejected/`; I recomputed those `.src` hashes and all four match the log, and I re-ran all six exclusion greps — the two date greps return `0` (why_duckdb, posthog) and the four marketing-framing greps return `1`, so no exclusion could have been written without opening the document.

**Handoff and constraints:** `PROGRESS.md` is current, well under budget, and states level 1 complete/awaiting evaluator with a concrete level-2 work list; `memory/INDEX.md` has exactly one summary line for each of the six files in `memory/`; `PAUSED_ACTIONS.md` records the blocked `rm -f`, whose consequence (the zero-byte `excluded-motherduck-vs.headers.txt`) is disclosed in both `CLAIM.md` and `CAPTURE_LOG.md` §3f. `git diff --name-only d418006 HEAD` shows 64 files, none with a `.py/.sql/.db/.duckdb/.sqlite/.parquet` extension — no binary was run and no measurement appears. The only blemish is the committed stray `sources/.fetchlog.tmp`, whose contents agree with the log; that is cosmetic and does not touch an acceptance criterion.
