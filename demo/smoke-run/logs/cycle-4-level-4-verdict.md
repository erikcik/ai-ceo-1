PASS

I opened and independently re-ran every artifact `evidence/level-4/CLAIM.md` names, and each acceptance line in `LEVELS.md` §Level 4 is satisfied by what the files actually show.

**Criterion 1 (`evidence/level-4/citation-audit.md`).** 49 rows, each carrying a `REPORT.md` line number, the opening words, kind, `key#Qn` or absence-check id, verdict and note; tally PASS 49 / FAIL 0 / UNCHECKED 0. I re-ran the file's own completeness one-liner (`awk '/^## Sources/{exit}...'`) against `REPORT.md` and got the sentence-start set `3 5 7 12 … 128`, which is byte-for-byte the audit's row-line set — no sentence escapes the table and no row points at a non-sentence. Spot-checking rows 116, 124, 12, 61 against `REPORT.md` confirmed the quoted opening words are the text at those lines, not retyped approximations. The distinct-`key#Qn` set extracted from the table is exactly 20, and per-key row counts (`duckdb-cidr20` 7, `sqlite-whentouse` 6, `duckdb-sigmod19` 6, `sqlite-wal` 5, `sqlite-lts` 5, `duckdb-pvldb-hashjoin` 5, `sqlite-cli` 1, `sqlite-changes` 1) reproduce the figures `CLAIM.md` reports in its residual-risks section.

**Criterion 2 (`evidence/level-4/verify.txt`).** All four required checks are present with real output, and my own re-runs reproduce them, satisfying Traceability descriptor 5 rather than just 4. I re-ran eight of the §1 canonical quote checks unmodified (`duckdb-pvldb-hashjoin#Q3`, `sqlite-wal#Q2`, `duckdb-cidr20#Q2`, `sqlite-lts#Q3`, `duckdb-sigmod19#Q1`, `sqlite-whentouse#Q4`, `sqlite-changes#Q2`, `sqlite-cli#Q1`) — all returned `1`, matching the transcript, including the curly-apostrophe cases. I re-ran `shasum -a 256 sources/raw/*.src sources/raw/*.txt`: all 24 hashes are identical to §2, and the `.src`/`.txt` values are the ones recorded in the `sources/<key>.md` headers. I independently re-derived two extractions (`pandoc -f html -t plain --wrap=none sources/raw/sqlite-wal.src` and `pdftotext sources/raw/duckdb-cidr20.src`) into my own scratch path and got `344d7480…` and `b48ccc47…`, matching the stored `.txt` exactly — the `.src → logged command → .txt` chain closes without the builder. Provenance is genuine, not composed: `sources/raw/sqlite-wal.src` opens with real `<!DOCTYPE html>` and sqlite.org nav/logo boilerplate, `sources/raw/sqlite-wal.headers.txt` carries `HTTP/1.1 200`, `Date`, `Last-Modified`, `ETag`, `Content-length: 41826`, and `duckdb-cidr20.src` begins `%PDF-1.5` with linearization objects. Word count re-run gives `1192`, in band.

**Criterion 3 (edits).** `git diff ca8f00f HEAD -- REPORT.md` shows exactly the two weakenings the audit's "Edits made this level" table names, at lines 116–119 and 124–125, with the before/after text matching character-for-character. I re-ran the checks that forced them: A1's quote-level grep over `sources/*.md` returns `0` while the capture-level counts confirm Parquet/JSON do occur in `duckdb-pvldb-hashjoin`, `sqlite-cli` and `sqlite-changes` — so "no source speaks to" really was false and "no accepted quote describes" really is true; A6 returns `sqlite-whentouse#Q3` alone, confirming the original "no SQLite-authored statement" clause contradicted the report's own line 33. Neither claim was kept unverified.

**Criterion 4 (level-3 criteria 1–4 still hold).** 1192 body words (band hard, verified by my own run); four numbered recommendation conditions at lines 83/89/95/101, each an observable of the reader's setup (installed RAM vs working set, count of OS processes writing the file, single-row updates vs bulk rebuilds, how long the file must stay readable and on what hardware) — none a workload adjective; 8 distinct keys cited, all resolving to `sources/<key>.md`; and I compared the `## Sources` block field-for-field against all eight entry headers — title, publisher, URL, `dated`, `accessed` agree in every case, so no Source-list-drift cap applies. `sqlite-changes#Q2`'s version attribution checks out too: line 977 of the capture is `2020-08-14 (3.33.0)` and line 993 is item 8 carrying the quote.

**Criterion 5 (`CLAIM.md` residual risks).** The risks named are the ones the artifacts support and are stated against the builder's own interest: single-document-per-axis with the per-key row counts I reproduced, the DuckDB side resting on 2019/2020/2025 papers because `duckdb.org/docs/stable/*` returned redirect stubs, the A2/A3 classifications flagged as judgement with hits printed, the `accessed: 2026-08-17` version-drift warning, the six unused claim rows, and the still-standing zero-byte `sources/rejected/excluded-motherduck-vs.headers.txt` whose `rm -f` the gate denied — which I confirmed is 0 bytes on disk and logged in `PAUSED_ACTIONS.md`. Nothing in the claim overstates the artifact.

**Constraints and handoff.** `evidence/level-4/make-level4-checks.sh` contains no invocation of any SQLite or DuckDB binary (the only matches are inside a filename-extension regex), and the level's diff adds no database, data or program source file. `PROGRESS.md` (~1000 words, well under budget) is current, names this level as done-and-awaiting-evaluator, and records the blocked action; `memory/INDEX.md` has exactly 11 bullets for exactly 11 files in `memory/`, including the new `auditing-a-finished-cited-report.md` lesson.
