NEEDS_WORK

**What I opened and re-ran (so the next session knows what is *not* in dispute):** `claims/dimensions.md` (7 dimensions, each with a concrete "What the reader can observe" paragraph), `claims/claim-map.md` (29 `OK` rows + 5 `W` rows), `claims/gaps.md` (7 gaps, each with a Consequence naming the cut row), `evidence/level-2/map-check.txt`, `evidence/level-2/make-map-check.sh`, `evidence/level-2/CLAIM.md`, all eight `sources/<key>.md` entries, `claims/excluded-sources.md`, `PROGRESS.md`, `memory/INDEX.md`, and `git diff fb91d2f..HEAD`. I independently reproduced the arithmetic (29 / 24 / 8, max 2 rows per `Qn`, 7/7/7), re-ran six canonical quote checks (`duckdb-pvldb-hashjoin#Q2`, `sqlite-wal#Q3`, `sqlite-whentouse#Q4`, `duckdb-cidr20#Q2`, `duckdb-sigmod19#Q1`, `sqlite-changes#Q2` → all `1`), re-ran the full sha256 reconciliation against `CAPTURE_LOG.md` (16/16 → `1`), re-ran the §6 fragment loop (no failures), confirmed the cited-vs-dossier set comparison is empty in both directions, verified the C22/C28 release-heading locators exist in `sources/raw/sqlite-changes.txt` (lines 977/993 and 652/662), and regenerated the whole transcript with `bash evidence/level-2/make-map-check.sh`, which came out **byte-identical apart from the generation-date line**. Criteria 1, 2, 3, 4, 5 and 7 are met, handoff is current (`PROGRESS.md` ~980 words, `memory/INDEX.md` exactly 8 lines for 8 memory files). The level fails on criterion 6 only:

- **Fails LEVELS.md L2 criterion 6 ("no claim row asserts a number … absent from the quote it cites") and Calibration ("no smuggled numbers") — `claims/claim-map.md` line 30, row C4.** The row ends: *"— a content-size bound that 5 GB is three orders of magnitude inside."* The cited quote (`sqlite-whentouse#Q4`, `sources/sqlite-whentouse.md` line 34) contains only "less than a terabyte of content"; the magnitude ratio is derived, not quoted, **and it is wrong**: 1 TB / 5 GB ≈ 200×, i.e. about 2.3 orders of magnitude, not three. This is exactly the class of unsourced figure level 3 would copy into `REPORT.md`. To satisfy: restate the row with a relation the quote and correct arithmetic support (e.g. "…a content-size bound far below which a 5 GB dataset sits", or an accurate "roughly 200× below"), leave `key#Qn` and the verbatim fragment unchanged, re-run `bash evidence/level-2/make-map-check.sh > evidence/level-2/map-check.txt`, and record the change in the "Rows tightened on re-read" table.
- **Fails the `CLAIM.md`-vs-artifact check (Calibration 5's clause: "where all are met, the `CLAIM.md` makes no assertion the artifact does not show") — `evidence/level-2/CLAIM.md` line 158.** It asserts: *"No figure, ratio, timing, or version number appears in any `OK` row that is not in its quote."* C4 carries precisely such a ratio, and the §5 grep the sentence relies on only searches for `Nx faster` / timings, so it cannot detect it. To satisfy: fix C4, then either narrow this sentence to what the §5 grep actually shows (no multipliers or timings) or state the residual derived-arithmetic cases explicitly.
- **Tighten while you are in the file (criterion 6, behaviour absent from the quote):** `claims/claim-map.md` line 54, row C13 appends "…and appending it to a persistent table **only if persistence is wanted**"; the cited span (`duckdb-cidr20#Q1`, and its surrounding context at `sources/raw/duckdb-cidr20.txt` lines 137–145) says the database "can directly scan existing files (e.g. CSV), reshape the result and then append it to a persistent table" — it states no optionality. Either drop the "only if persistence is wanted" clause or bind it to a quote that says it. (`sources/raw/duckdb-cidr20.txt` line 144 is about desirability, not optional persistence.) Row C21 (line 72) adds "rather than overwriting pages in place" to "Writers merely append new content to the end of the WAL file"; that one is a fair reading of "merely append" — flagged only so you do not spend time on it.
