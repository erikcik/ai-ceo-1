# evidence/level-3/claim-coverage.md -- generated, do not hand-edit

Generated by `evidence/level-3/make-level3-checks.sh` from `REPORT.md` itself: every
sentence below was **extracted from the report at the line number given**, not retyped, so
the location and the text cannot drift apart. `evidence/level-3/verify.txt` §1 then greps
each extracted sentence back against `REPORT.md` and prints the count.

"Line" is the line the sentence *starts* on; report sentences are written one per line
(semantic line breaks), wrapped at 100 columns, so a sentence may span 2-3 physical lines.

| line | claim | key#Qn | exact sentence text in REPORT.md |
|---|---|---|---|
| 12 | C3 | `sqlite-whentouse#Q2` | SQLite keeps the entire database in one disk file, so the host filesystem's maximum file size can bind before SQLite's own limits do [sqlite-whentouse]. |
| 14 | C4 | `sqlite-whentouse#Q4` | The SQLite project's own checklist recommends SQLite for device-local storage with low writer concurrency and less than a terabyte of content — a content-size bound far below which a 5 GB dataset sits [sqlite-whentouse]. |
| 17 | C1 | `duckdb-pvldb-hashjoin#Q3` | A 2025 PVLDB paper describes DuckDB storing query intermediates on pages, which lets the buffer manager evict them to storage when they exceed the available memory limit [duckdb-pvldb-hashjoin]. |
| 20 | C2 | `duckdb-pvldb-hashjoin#Q1` | For memory-intensive join plans that paper's authors report that their implementation degrades gracefully as the space requirement exceeds the memory limit — a statement about the shape of the degradation, not about throughput [duckdb-pvldb-hashjoin]. |
| 26 | C5 | `duckdb-sigmod19#Q2` | DuckDB executes queries with a vectorized interpreted execution engine, chosen over JIT compilation for portability [duckdb-sigmod19]. |
| 28 | C20 | `duckdb-sigmod19#Q1` | DuckDB's SIGMOD 2019 paper characterises SQLite as containing a row-major execution engine operating on a B-tree storage format, and presents its analytical performance as a consequence of that design [duckdb-sigmod19]. |
| 31 | C6 | `duckdb-sigmod19#Q1` | The same paper states that SQLite focuses on transactional workloads and that its performance on analytical workloads is very poor [duckdb-sigmod19]. |
| 33 | C7 | `sqlite-whentouse#Q3` | The SQLite project itself lists analysis of large datasets through the `sqlite3` command-line shell as a situation where SQLite works well [sqlite-whentouse]. |
| 38 | C8 | `sqlite-whentouse#Q1` | SQLite supports an unlimited number of simultaneous readers but allows only one writer at any instant [sqlite-whentouse]. |
| 40 | C9 | `sqlite-wal#Q1` | In WAL mode readers do not block writers and a writer does not block readers, so reading and writing proceed concurrently [sqlite-wal]. |
| 42 | C10 | `sqlite-wal#Q2` | WAL mode does not lift the single-writer limit: because there is only one WAL file, there can be only one writer at a time [sqlite-wal]. |
| 44 | C11 | `sqlite-wal#Q3` | WAL mode also requires every process using the database to be on the same host computer; it does not work over a network filesystem [sqlite-wal]. |
| 46 | C12 | `duckdb-sigmod19#Q3` | For DuckDB this dossier reaches only the concurrency-control model: the SIGMOD 2019 paper implements a serializable variant of MVCC tailored specifically for hybrid OLAP/OLTP systems [duckdb-sigmod19]. |
| 53 | C14 | `sqlite-cli#Q1` | In SQLite, `.import` reads a delimited file into a table; if the target table does not exist it is created and the first input row supplies the column names [sqlite-cli]. |
| 55 | C13 | `duckdb-cidr20#Q1` | DuckDB's CIDR 2020 paper describes the database directly scanning existing files such as CSV, reshaping the result, and appending it to a persistent table [duckdb-cidr20]. |
| 57 | C17 | `duckdb-cidr20#Q2` | DuckDB's single-file storage format is designed, that same paper says, to support efficient scans and bulk updates, appends and deletes [duckdb-cidr20]. |
| 59 | C18 | `duckdb-cidr20#Q2` | Single-value and single-row updates are supported by that format, but the paper states their efficiency is explicitly not a design goal [duckdb-cidr20]. |
| 61 | C22 | `sqlite-changes#Q2` | On durability, SQLite's release history records a 2020 change (3.33.0) after which, in WAL mode, transactions can recover an inconsistent shm file left by a crashed writer even while read transactions are active [sqlite-changes]. |
| 64 | C23 | `duckdb-sigmod19#Q3` | DuckDB's SIGMOD 2019 paper claims ACID compliance through MVCC [duckdb-sigmod19]. |
| 68 | C24 | `sqlite-lts#Q1` | SQLite database files are bit-for-bit identical across 32-bit, 64-bit, big-endian and little-endian platforms and can be copied between systems without translation or conversion [sqlite-lts]. |
| 71 | C25 | `sqlite-lts#Q2` | The project states that its file format is well documented and stable and that files created today will be readable and writable by future versions decades from now, and its developers state an intent to support SQLite through the year 2050 [sqlite-lts]. |
| 71 | C26 | `sqlite-lts#Q3` | The project states that its file format is well documented and stable and that files created today will be readable and writable by future versions decades from now, and its developers state an intent to support SQLite through the year 2050 [sqlite-lts]. |
| 74 | C29 | `duckdb-pvldb-hashjoin#Q2` | DuckDB ships numbered releases, and its authors describe it in that 2025 peer-reviewed paper as no research prototype but a widely used and well-tested system, with the paper's techniques available in the v1.2.0 release [duckdb-pvldb-hashjoin]. |

**23 rows, 23 distinct claim ids, 8 distinct source keys.** Level 3 criterion 5 asks for
at least 12 claim rows from `claims/claim-map.md` to appear in the report; criterion 4 asks
for at least 6 distinct keys cited.

Two rows share line 71: that one sentence carries both C25 (format stable, readable
decades from now) and C26 (support intended through 2050), which are separate rows in the
claim map resting on separate quotes (`sqlite-lts#Q2` and `#Q3`).

## Claims restated in the recommendation

These are the same claim rows argued a second time as decision conditions. They are **not**
counted in the totals above; the rows above are where each claim is first asserted.

| line | claim restated | key#Qn |
|---|---|---|
| 90 | C8 + C10 (one writer at any instant; WAL does not lift it) | `sqlite-whentouse#Q1`, `sqlite-wal#Q2` |
| 91 | C11 (WAL does not work over a network filesystem) | `sqlite-wal#Q3` |
| 97 | C18 (single-row update efficiency is not a design goal) | `duckdb-cidr20#Q2` |
| 99 | C17 (format designed for scans and bulk updates) | `duckdb-cidr20#Q2` |
| 103 | C24-C26 (cross-platform file, stable format, support to 2050) | `sqlite-lts#Q1-Q3` |
| 106 | C18 again, as the stated cost of choosing DuckDB | `duckdb-cidr20#Q2` |
| 110 | C13 (DuckDB scanning CSV files directly) | `duckdb-cidr20#Q1` |

## Claim-map rows deliberately not used

Six of the 29 `OK` rows did not fit the 900-1200 word band and are absent from the report:
C15 (import delimiters), C16 (CSV imported then sliced into summary reports), C19 (DuckDB's
single-file design inspired by SQLite), C21 (WAL writers append rather than overwrite),
C27 (the changelog page describes itself), C28 (a 2022 interface change held backwards
compatible). Dropping them costs no dimension its coverage: D4 keeps C13/C14, D5 keeps
C17/C18, D6 keeps C22/C23, D7 keeps C24/C25/C26/C29.
