# evidence/level-2/map-check.txt

Verification transcript for level 2, acceptance criteria 3, 4, 6 and 7.

Every block below is a copy-pasteable one-liner followed by its real output. The file is
produced by `evidence/level-2/make-map-check.sh`, which runs each command and pastes what it
printed; the quote strings in §1 are read out of the source entries at generation time, so
this transcript cannot drift from the entry files. Re-run the script from the repository
root and only the generation-date line at the foot should change.

What each section proves:
  §1  every distinct key#Qn in claims/claim-map.md resolves to a real quote in a real
      capture  ..................................................  criterion 4
  §2  the map arithmetic: row count, distinct key#Qn, distinct keys, rows-per-Qn cap,
      per-dimension and per-dimension-per-system coverage  .......  criterion 3
  §3  every sources/<key>.md entry is cited by at least one claim row  ..  criterion 7
  §4  no claim row cites a document from claims/excluded-sources.md  ...  criterion 6
  §5  the W1 Parquet negative check, and an exhaustive audit of every number in every
      OK claim sentence against the quote, locator and date behind it  ..  criterion 6
  §6  every row's verbatim fragment lies inside the quote that row cites  ..  criterion 2
  §7  the captures the quotes were checked against are the ones level 1 logged  (sha256)

The canonical quote check is the one the evidence taxonomy fixes in its §1.4:

    tr -s '[:space:]' ' ' < sources/raw/<key>.txt | grep -F -c "<quote on one line>"

It is run below with the FULL text of each quote, not a fragment, so a pass means the
entire quoted span is present in the capture verbatim after whitespace collapsing.

==============================================================================
## §1 - every distinct key#Qn resolves: entry -> quote -> capture
==============================================================================

Two commands per quote. The first shows the quote label exists in the source entry file
(criterion 4, "every Qn exists in that entry"); the second shows that quote's full text
exists in that entry's stored capture.
Distinct key#Qn cited by OK claim rows: 24. Quotes present in the dossier: 24.
Pairs cited but absent from the dossier (must be empty):
Quotes in the dossier that no claim row cites (must be empty; criterion 7):

------------------------------------------------------------------------------
### duckdb-cidr20#Q1   (locator recorded in entry: Q1 — §"INTRODUCTION" (ETL/embedded analytics argument), page 2)
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/duckdb-cidr20.md
29:### Q1 — §"INTRODUCTION" (ETL/embedded analytics argument), page 2

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-cidr20.txt | grep -F -c "Feasible because the database can directly scan existing files (e.g. CSV), reshape the result and then append it to a persistent table."
1

------------------------------------------------------------------------------
### duckdb-cidr20#Q2   (locator recorded in entry: Q2 — §"THE DUCKDB SYSTEM", storage subsection, page 4)
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/duckdb-cidr20.md
32:### Q2 — §"THE DUCKDB SYSTEM", storage subsection, page 4

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-cidr20.txt | grep -F -c "DuckDB uses a single-file storage format to store data on disk. The file format is designed to support efficient scans and bulk updates, appends and deletes. While single-value or single-row updates are supported, their efficiency is not a design goal."
1

------------------------------------------------------------------------------
### duckdb-cidr20#Q3   (locator recorded in entry: Q3 — §"THE DUCKDB SYSTEM", storage subsection, page 4)
------------------------------------------------------------------------------
$ grep -n '^### Q3 ' sources/duckdb-cidr20.md
35:### Q3 — §"THE DUCKDB SYSTEM", storage subsection, page 4

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-cidr20.txt | grep -F -c "The fact that the database only consists of a single file was inspired by SQLite [3] and repeated user requests for this feature in MonetDBLite."
1

------------------------------------------------------------------------------
### duckdb-pvldb-hashjoin#Q1   (locator recorded in entry: Q1 — Abstract, page 1)
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/duckdb-pvldb-hashjoin.md
31:### Q1 — Abstract, page 1

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-pvldb-hashjoin.txt | grep -F -c "We integrate these techniques into DuckDB and experimentally show that when processing memory-intensive join query plans, our implementation gracefully degrades performance as the space requirement exceeds the memory limit."
1

------------------------------------------------------------------------------
### duckdb-pvldb-hashjoin#Q2   (locator recorded in entry: Q2 — §1 "Introduction", contributions, page 2)
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/duckdb-pvldb-hashjoin.md
34:### Q2 — §1 "Introduction", contributions, page 2

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-pvldb-hashjoin.txt | grep -F -c "All three are available in the v1.2.0 release. DuckDB is no research prototype but a widely used and well-tested system."
1

------------------------------------------------------------------------------
### duckdb-pvldb-hashjoin#Q3   (locator recorded in entry: Q3 — §3 "MANAGING TEMPORARY DATA", paragraph "Unified Memory Management", page 3)
------------------------------------------------------------------------------
$ grep -n '^### Q3 ' sources/duckdb-pvldb-hashjoin.md
37:### Q3 — §3 "MANAGING TEMPORARY DATA", paragraph "Unified Memory Management", page 3

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-pvldb-hashjoin.txt | grep -F -c "DuckDB’s buffer manager not only uses paged allocations for persistent data but also for temporary data. Storing intermediates on pages allows the buffer manager to evict them to storage if intermediates exceed the available memory limit."
1

------------------------------------------------------------------------------
### duckdb-sigmod19#Q1   (locator recorded in entry: Q1 — §1 "Introduction", page 1)
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/duckdb-sigmod19.md
30:### Q1 — §1 "Introduction", page 1

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-sigmod19.txt | grep -F -c "SQLite strongly focuses on transactional (OLTP) workloads, and contains a row-major execution engine operating on a B-Tree storage format [3]. As a consequence, SQLite’s performance on analytical (OLAP) workloads is very poor."
1

------------------------------------------------------------------------------
### duckdb-sigmod19#Q2   (locator recorded in entry: Q2 — §"DESIGN AND IMPLEMENTATION" (execution engine paragraph), page 3)
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/duckdb-sigmod19.md
33:### Q2 — §"DESIGN AND IMPLEMENTATION" (execution engine paragraph), page 3

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-sigmod19.txt | grep -F -c "DuckDB uses a vectorized interpreted execution engine [1]. This approach was chosen over Just-in-Time compilation (JIT) of SQL queries [8] for portability reasons."
1

------------------------------------------------------------------------------
### duckdb-sigmod19#Q3   (locator recorded in entry: Q3 — §"DESIGN AND IMPLEMENTATION" (transaction manager paragraph), page 3)
------------------------------------------------------------------------------
$ grep -n '^### Q3 ' sources/duckdb-sigmod19.md
36:### Q3 — §"DESIGN AND IMPLEMENTATION" (transaction manager paragraph), page 3

$ tr -s '[:space:]' ' ' < sources/raw/duckdb-sigmod19.txt | grep -F -c "DuckDB provides ACID-compliance through Multi-Version Concurrency Control (MVCC). We implement HyPer’s serializable variant of MVCC that is tailored specifically for hybrid OLAP/OLTP systems [10]."
1

------------------------------------------------------------------------------
### sqlite-changes#Q1   (locator recorded in entry: Q1 — §"Release History", opening paragraph)
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/sqlite-changes.md
26:### Q1 — §"Release History", opening paragraph

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-changes.txt | grep -F -c "This page provides a high-level summary of changes to SQLite. For more detail, see the Fossil checkin logs at https://sqlite.org/src/timeline and https://sqlite.org/src/timeline?t=release."
1

------------------------------------------------------------------------------
### sqlite-changes#Q2   (locator recorded in entry: Q2 — release entry "2020-08-14 (3.33.0)", item 8)
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/sqlite-changes.md
29:### Q2 — release entry "2020-08-14 (3.33.0)", item 8

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-changes.txt | grep -F -c "In WAL mode, if a writer crashes and leaves the shm file in an inconsistent state, subsequent transactions are now able to recover the shm file even if there are active read transactions."
1

------------------------------------------------------------------------------
### sqlite-changes#Q3   (locator recorded in entry: Q3 — release entry "2022-11-16 (3.40.0)", item 4)
------------------------------------------------------------------------------
$ grep -n '^### Q3 ' sources/sqlite-changes.md
32:### Q3 — release entry "2022-11-16 (3.40.0)", item 4

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-changes.txt | grep -F -c "This interface change should be fully backwards compatible, though it might cause (harmless) compiler warnings when rebuilding some legacy applications."
1

------------------------------------------------------------------------------
### sqlite-cli#Q1   (locator recorded in entry: Q1 — §"7.5. Importing files as CSV or other formats")
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/sqlite-cli.md
25:### Q1 — §"7.5. Importing files as CSV or other formats"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-cli.txt | grep -F -c "When .import is run, its treatment of the first input row depends upon whether the target table already exists. If it does not exist, the table is automatically created and the content of the first input row is used to set the name of all the columns in the table."
1

------------------------------------------------------------------------------
### sqlite-cli#Q2   (locator recorded in entry: Q2 — §"7.5. Importing files as CSV or other formats")
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/sqlite-cli.md
28:### Q2 — §"7.5. Importing files as CSV or other formats"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-cli.txt | grep -F -c "If the --csv or --ascii options are used, they control import input delimiters. Otherwise, the delimiters are those in effect for the current output mode."
1

------------------------------------------------------------------------------
### sqlite-lts#Q1   (locator recorded in entry: Q1 — §"Stable, Cross-platform Database Files")
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/sqlite-lts.md
23:### Q1 — §"Stable, Cross-platform Database Files"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-lts.txt | grep -F -c "SQLite database files are bit-for-bit identical on 32-bit, 64-bit, big-endian, and little-endian platforms. You can copy an SQLite database file from one system to another without having to translate or convert the database."
1

------------------------------------------------------------------------------
### sqlite-lts#Q2   (locator recorded in entry: Q2 — §"Stable, Cross-platform Database Files")
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/sqlite-lts.md
26:### Q2 — §"Stable, Cross-platform Database Files"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-lts.txt | grep -F -c "Furthermore, the file format is well documented and stable. Database files created today will be readable and writable by future versions of SQLite decades in the future."
1

------------------------------------------------------------------------------
### sqlite-lts#Q3   (locator recorded in entry: Q3 — opening paragraph, under heading "Long Term Support")
------------------------------------------------------------------------------
$ grep -n '^### Q3 ' sources/sqlite-lts.md
29:### Q3 — opening paragraph, under heading "Long Term Support"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-lts.txt | grep -F -c "The intent of the developers is to support SQLite through the year 2050."
1

------------------------------------------------------------------------------
### sqlite-wal#Q1   (locator recorded in entry: Q1 — §"1. Overview", advantages list, item 2)
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/sqlite-wal.md
26:### Q1 — §"1. Overview", advantages list, item 2

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-wal.txt | grep -F -c "WAL provides more concurrency as readers do not block writers and a writer does not block readers. Reading and writing can proceed concurrently."
1

------------------------------------------------------------------------------
### sqlite-wal#Q2   (locator recorded in entry: Q2 — §"2. How WAL Works")
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/sqlite-wal.md
29:### Q2 — §"2. How WAL Works"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-wal.txt | grep -F -c "Writers merely append new content to the end of the WAL file. Because writers do nothing that would interfere with the actions of readers, writers and readers can run at the same time. However, since there is only one WAL file, there can only be one writer at a time."
1

------------------------------------------------------------------------------
### sqlite-wal#Q3   (locator recorded in entry: Q3 — §"1. Overview", disadvantages list, item 1)
------------------------------------------------------------------------------
$ grep -n '^### Q3 ' sources/sqlite-wal.md
32:### Q3 — §"1. Overview", disadvantages list, item 1

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-wal.txt | grep -F -c "All processes using a database must be on the same host computer; WAL does not work over a network filesystem."
1

------------------------------------------------------------------------------
### sqlite-whentouse#Q1   (locator recorded in entry: Q1 — §"2. Situations Where A Client/Server RDBMS May Work Better" → "High Concurrency")
------------------------------------------------------------------------------
$ grep -n '^### Q1 ' sources/sqlite-whentouse.md
24:### Q1 — §"2. Situations Where A Client/Server RDBMS May Work Better" → "High Concurrency"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-whentouse.txt | grep -F -c "SQLite supports an unlimited number of simultaneous readers, but it will only allow one writer at any instant in time."
1

------------------------------------------------------------------------------
### sqlite-whentouse#Q2   (locator recorded in entry: Q2 — §"2. Situations Where A Client/Server RDBMS May Work Better" → "Very large datasets")
------------------------------------------------------------------------------
$ grep -n '^### Q2 ' sources/sqlite-whentouse.md
27:### Q2 — §"2. Situations Where A Client/Server RDBMS May Work Better" → "Very large datasets"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-whentouse.txt | grep -F -c "And even if it could handle larger databases, SQLite stores the entire database in a single disk file and many filesystems limit the maximum size of files to something less than this."
1

------------------------------------------------------------------------------
### sqlite-whentouse#Q3   (locator recorded in entry: Q3 — §"1. Situations Where SQLite Works Well" → "Data analysis")
------------------------------------------------------------------------------
$ grep -n '^### Q3 ' sources/sqlite-whentouse.md
30:### Q3 — §"1. Situations Where SQLite Works Well" → "Data analysis"

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-whentouse.txt | grep -F -c "People who understand SQL can employ the sqlite3 command-line shell (or various third-party SQLite access programs) to analyze large datasets. Raw data can be imported from CSV files, then that data can be sliced and diced to generate a myriad of summary reports."
1

------------------------------------------------------------------------------
### sqlite-whentouse#Q4   (locator recorded in entry: Q4 — §"3. Checklist For Choosing The Right Database Engine", item 4 ("Otherwise → choose SQLite!"))
------------------------------------------------------------------------------
$ grep -n '^### Q4 ' sources/sqlite-whentouse.md
33:### Q4 — §"3. Checklist For Choosing The Right Database Engine", item 4 ("Otherwise → choose SQLite!")

$ tr -s '[:space:]' ' ' < sources/raw/sqlite-whentouse.txt | grep -F -c "For device-local storage with low writer concurrency and less than a terabyte of content, SQLite is almost always a better solution."
1

24 distinct key#Qn pairs checked, 2 commands each.

==============================================================================
## §2 - map arithmetic (criterion 3)
==============================================================================
Claim rows with status OK (criterion 3 requires >= 14):
$ grep -E "\| OK \|$" claims/claim-map.md | wc -l | tr -d ' '
29

Distinct key#Qn pairs across those rows (requires >= 10):
$ grep -E "\| OK \|$" claims/claim-map.md | grep -oE '`[a-z0-9-]+#Q[0-9]+`' | sort -u | wc -l | tr -d ' '
24

Distinct source keys across those rows (requires >= 6):
$ grep -E "\| OK \|$" claims/claim-map.md | grep -oE '`[a-z0-9-]+#Q[0-9]+`' | sed 's/#Q[0-9]*`/`/' | sort -u | wc -l | tr -d ' '
8

Rows per Qn, most-used first (requires no Qn above 3):
$ grep -E "\| OK \|$" claims/claim-map.md | grep -oE '`[a-z0-9-]+#Q[0-9]+`' | sort | uniq -c | sort -rn | head -6
   2 `sqlite-whentouse#Q3`
   2 `sqlite-wal#Q2`
   2 `duckdb-sigmod19#Q3`
   2 `duckdb-sigmod19#Q1`
   2 `duckdb-cidr20#Q2`
   1 `sqlite-whentouse#Q4`

Rows per dimension (requires every dimension D1-D7 to have >= 1):
$ grep -E "\| OK \|$" claims/claim-map.md | awk -F'|' '{gsub(/ /,"",$3); print $3}' | sort | uniq -c
   4 D1
   3 D2
   5 D3
   4 D4
   4 D5
   3 D6
   6 D7

Rows per dimension per system (requires >= 1 row per system in each dimension where
the systems differ; here every dimension carries both):
$ grep -E "\| OK \|$" claims/claim-map.md | awk -F'|' '{gsub(/ /,"",$3); gsub(/ /,"",$5); print $3, $5}' | sort | uniq -c
   2 D1 duckdb
   2 D1 sqlite
   1 D2 duckdb
   2 D2 sqlite
   1 D3 duckdb
   4 D3 sqlite
   1 D4 duckdb
   3 D4 sqlite
   3 D5 duckdb
   1 D5 sqlite
   1 D6 duckdb
   2 D6 sqlite
   1 D7 duckdb
   5 D7 sqlite

Rows carried as WEAKENED or DROPPED (not counted toward the 14):
$ grep -cE '\| (WEAKENED|DROPPED) \|$' claims/claim-map.md
5

Dimensions defined in claims/dimensions.md (criterion 1 requires 6-9):
$ grep -cE '^## D[0-9]+ ' claims/dimensions.md
7

Each dimension states what the reader can observe (must equal the dimension count):
$ grep -cE '^\*\*What the reader can observe' claims/dimensions.md
7

Questions listed in claims/gaps.md (criterion 5 requires >= 3):
$ grep -cE '^## [0-9]+\. ' claims/gaps.md
7

==============================================================================
## §3 - dossier closure: every entry is cited by >= 1 claim row (criterion 7)
==============================================================================

For each sources/<key>.md, the number of OK claim rows citing it. A zero would require that
entry to be moved to sources/unused/ with a note; there are none, so sources/unused/ does
not exist.
$ for k in $(ls sources/*.md | xargs -n1 basename | sed 's/\.md$//' | grep -v CAPTURE_LOG); do printf '%-24s %s\n' "$k" "$(grep -E "\| OK \|$" claims/claim-map.md | grep -c "\`$k#Q")"; done
duckdb-cidr20            4
duckdb-pvldb-hashjoin    3
duckdb-sigmod19          5
sqlite-changes           3
sqlite-cli               2
sqlite-lts               3
sqlite-wal               4
sqlite-whentouse         5

Confirming sources/unused/ is absent (exit status 1 and "No such file" is the expected
result, because no entry went uncited):
$ ls -d sources/unused 2>&1; echo "exit=$?"
ls: sources/unused: No such file or directory
exit=1

And that every key cited in the map is a file that exists on disk:
$ grep -E "\| OK \|$" claims/claim-map.md | grep -oE '`[a-z0-9-]+#Q[0-9]+`' | tr -d '`' | sed 's/#Q[0-9]*//' | sort -u | while read k; do printf '%-24s %s\n' "$k" "$(test -f sources/$k.md && echo FOUND || echo MISSING)"; done
duckdb-cidr20            FOUND
duckdb-pvldb-hashjoin    FOUND
duckdb-sigmod19          FOUND
sqlite-changes           FOUND
sqlite-cli               FOUND
sqlite-lts               FOUND
sqlite-wal               FOUND
sqlite-whentouse         FOUND

And the other direction of criterion 7 - the distinct source keys each dimension rests
on, so no dimension is left without a primary source:
$ grep -E "\| OK \|$" claims/claim-map.md | awk -F'|' '{gsub(/ /,"",$3); gsub(/`/,"",$6); gsub(/ /,"",$6); sub(/#Q[0-9]+/,"",$6); print $3" "$6}' | sort -u | awk '{a[$1]=a[$1]" "$2} END{for(d in a) print d":"a[d]}' | sort
D1: duckdb-pvldb-hashjoin sqlite-whentouse
D2: duckdb-sigmod19 sqlite-whentouse
D3: duckdb-sigmod19 sqlite-wal sqlite-whentouse
D4: duckdb-cidr20 sqlite-cli sqlite-whentouse
D5: duckdb-cidr20 duckdb-sigmod19
D6: duckdb-sigmod19 sqlite-changes sqlite-wal
D7: duckdb-pvldb-hashjoin sqlite-changes sqlite-lts

==============================================================================
## §4 - no claim row cites an excluded document (criterion 6)
==============================================================================

The keys in claims/excluded-sources.md, counted against every claim row in
claims/claim-map.md (rows beginning "| C<n> |" or "| W<n> |"). All must be 0.
$ for k in excluded-why-duckdb excluded-posthog-vs excluded-betterstack-vs excluded-lukasbarth-bench; do printf '%-26s %s\n' "$k" "$(grep -E '^\| [CW][0-9]+ \|' claims/claim-map.md | grep -c "$k")"; done
excluded-why-duckdb        0
excluded-posthog-vs        0
excluded-betterstack-vs    0
excluded-lukasbarth-bench  0

The excluded keys as they are listed in claims/excluded-sources.md, for comparison:
$ grep -oE 'excluded-[a-z-]+' claims/excluded-sources.md | sort -u
excluded-betterstack-vs
excluded-lukasbarth-bench
excluded-posthog-vs
excluded-sources
excluded-why-duckdb

==============================================================================
## §5 - no number is smuggled into a claim row (and the W1 Parquet negative check)
==============================================================================

W1 claims nothing because nothing supports it. Occurrences of "Parquet" (case-insensitive)
in each capture:
$ for f in sources/raw/*.txt; do printf '%-45s %s\n' "$f" "$(grep -ic parquet "$f")"; done
sources/raw/duckdb-cidr20.headers.txt         0
sources/raw/duckdb-cidr20.txt                 0
sources/raw/duckdb-pvldb-hashjoin.headers.txt 0
sources/raw/duckdb-pvldb-hashjoin.txt         1
sources/raw/duckdb-sigmod19.headers.txt       0
sources/raw/duckdb-sigmod19.txt               0
sources/raw/sqlite-changes.headers.txt        0
sources/raw/sqlite-changes.txt                0
sources/raw/sqlite-cli.headers.txt            0
sources/raw/sqlite-cli.txt                    0
sources/raw/sqlite-lts.headers.txt            0
sources/raw/sqlite-lts.txt                    0
sources/raw/sqlite-wal.headers.txt            0
sources/raw/sqlite-wal.txt                    0
sources/raw/sqlite-whentouse.headers.txt      0
sources/raw/sqlite-whentouse.txt              0

The single occurrence, in context - a future-work sentence about join plans, not about
ingest:
$ grep -n -B1 -A2 -i parquet sources/raw/duckdb-pvldb-hashjoin.txt
112-This would then reduce the cost of challenging analytical workloads
113:such as large self-joins, join plans including Parquet files, which
114-are difficult to reorder because only lightweight statistics [30] are
115-available, and join queries with strings, which are ubiquitous in

And no speed figure was smuggled into the map either - rows carrying a multiplier or a
timing (must be empty):
$ grep -nE '[0-9]+(\.[0-9]+)? ?(x|times) (faster|slower)|[0-9]+ ?(ms|milliseconds|seconds|minutes)' claims/claim-map.md

That grep only catches multipliers and timings. Criterion 6 is wider than that: it forbids
ANY number in a claim sentence that is absent from the quote the row cites. So the check
below is exhaustive rather than pattern-based - it extracts every numeric token from the
claim column of every OK row and asks three questions of each:

  in-quote    does the number appear in the text of the quote this row cites?
  in-locator  does it appear in that quote's locator heading in sources/<key>.md
              (this is how a release number or release year is sourced)?
  in-dated    does it appear in the cited entry's `dated:` header field (this is how a
              paper's publication year is sourced, e.g. "DuckDB's SIGMOD 2019 paper")?

A row is clean if at least one column is 1. Anything that is 0/0/0 is a number the map
asserts on its own authority and must be justified by name in evidence/level-2/CLAIM.md.
The number is matched on numeric boundaries, not as a bare substring, so the 5 of "5 GB"
cannot count itself sourced by finding a 5 inside a 2025 date.
$ grep -E '\| OK \|$' claims/claim-map.md | awk -F'|' '{gsub(/^ +| +$/,"",$2);gsub(/^ +| +$/,"",$6);gsub(/`/,"",$6); n=$4; while (match(n,/[0-9][0-9.]*[0-9]|[0-9]/)) { t=substr(n,RSTART,RLENGTH); print $2"\t"$6"\t"t; n=substr(n,RSTART+RLENGTH) } }' | while IFS=$'\t' read -r id kq num; do k=${kq%%#*}; q=${kq##*#}; qt=$(grep -A1 "^### $q " "sources/$k.md" | sed -n '2s/^> //p'); loc=$(grep -m1 "^### $q " "sources/$k.md"); dt=$(grep -m1 '^dated:' "sources/$k.md"); re="(^|[^0-9.])$(printf '%s' "$num" | sed 's/\./\\./g')([^0-9]|\$)"; printf '%-5s %-28s %-8s in-quote=%s in-locator=%s in-dated=%s\n' "$id" "$kq" "$num" "$(printf '%s' "$qt" | grep -Ec "$re")" "$(printf '%s' "$loc" | grep -Ec "$re")" "$(printf '%s' "$dt" | grep -Ec "$re")"; done
C4    sqlite-whentouse#Q4          5        in-quote=0 in-locator=0 in-dated=0
C6    duckdb-sigmod19#Q1           2019     in-quote=0 in-locator=0 in-dated=1
C7    sqlite-whentouse#Q3          3        in-quote=1 in-locator=1 in-dated=0
C13   duckdb-cidr20#Q1             2020     in-quote=0 in-locator=0 in-dated=1
C20   duckdb-sigmod19#Q1           2019     in-quote=0 in-locator=0 in-dated=1
C22   sqlite-changes#Q2            2020     in-quote=0 in-locator=1 in-dated=0
C22   sqlite-changes#Q2            3.33.0   in-quote=0 in-locator=1 in-dated=0
C23   duckdb-sigmod19#Q3           2019     in-quote=0 in-locator=0 in-dated=1
C24   sqlite-lts#Q1                32       in-quote=1 in-locator=0 in-dated=0
C24   sqlite-lts#Q1                64       in-quote=1 in-locator=0 in-dated=0
C26   sqlite-lts#Q3                2050     in-quote=1 in-locator=0 in-dated=0
C28   sqlite-changes#Q3            2022     in-quote=0 in-locator=1 in-dated=0
C29   duckdb-pvldb-hashjoin#Q2     2025     in-quote=0 in-locator=0 in-dated=1
C29   duckdb-pvldb-hashjoin#Q2     1.2.0    in-quote=1 in-locator=0 in-dated=0

The same audit, printing only the numbers that are 0/0/0 - unsourced by quote, locator or
entry date. Each one that remains is named and justified in evidence/level-2/CLAIM.md:
$ grep -E '\| OK \|$' claims/claim-map.md | awk -F'|' '{gsub(/^ +| +$/,"",$2);gsub(/^ +| +$/,"",$6);gsub(/`/,"",$6); n=$4; while (match(n,/[0-9][0-9.]*[0-9]|[0-9]/)) { t=substr(n,RSTART,RLENGTH); print $2"\t"$6"\t"t; n=substr(n,RSTART+RLENGTH) } }' | while IFS=$'\t' read -r id kq num; do k=${kq%%#*}; q=${kq##*#}; qt=$(grep -A1 "^### $q " "sources/$k.md" | sed -n '2s/^> //p'); loc=$(grep -m1 "^### $q " "sources/$k.md"); dt=$(grep -m1 '^dated:' "sources/$k.md"); re="(^|[^0-9.])$(printf '%s' "$num" | sed 's/\./\\./g')([^0-9]|\$)"; a=$(printf '%s' "$qt" | grep -Ec "$re"); b=$(printf '%s' "$loc" | grep -Ec "$re"); c=$(printf '%s' "$dt" | grep -Ec "$re"); [ "$a$b$c" = 000 ] && printf '%-5s %-28s %s\n' "$id" "$kq" "$num"; done; true
C4    sqlite-whentouse#Q4          5

And a grep for derived-ratio language - orders of magnitude, N-fold, N-times-below,
percent - across every claim row, OK and cut alike (must be empty; this is the class of
wording that got C4 narrowed twice). The filter selects rows by their status column, so the
"rows tightened on re-read" table, which quotes the removed wording on purpose, is not
searched here - it is meant to contain it:
$ grep -E '\| (OK|WEAKENED|DROPPED) \|$' claims/claim-map.md | grep -nEi 'orders? of magnitude|[0-9]+ ?[x×] (below|above|larger|smaller|more|less|faster|slower)|[0-9]+ ?-?fold|[0-9]+ ?%'; true

==============================================================================
## §6 - every claim row's verbatim fragment lies inside the quote it cites
==============================================================================

Criterion 2 requires the map to carry "a short verbatim fragment of the supporting quote".
This check does not test the fragment against the capture (§1 already did that for the full
quote); it tests the fragment against the *specific quote the row cites*, by pulling that
quote out of sources/<key>.md and grepping the fragment against it. A 1 means the row could
not have picked its fragment from some other passage of the document.
$ grep -E "\| OK \|$" claims/claim-map.md | awk -F'|' '{gsub(/^ +| +$/,"",$2);gsub(/^ +| +$/,"",$6);gsub(/^ +| +$/,"",$7);gsub(/`/,"",$6);print $2"\t"$6"\t"$7}' | while IFS=$'\t' read -r id kq frag; do k=${kq%%#*}; q=${kq##*#}; qt=$(grep -A1 "^### $q " "sources/$k.md" | sed -n '2s/^> //p'); printf '%-5s %-30s %s\n' "$id" "$kq" "$(printf '%s' "$qt" | grep -F -c "$frag")"; done
C1    duckdb-pvldb-hashjoin#Q3       1
C2    duckdb-pvldb-hashjoin#Q1       1
C3    sqlite-whentouse#Q2            1
C4    sqlite-whentouse#Q4            1
C5    duckdb-sigmod19#Q2             1
C6    duckdb-sigmod19#Q1             1
C7    sqlite-whentouse#Q3            1
C8    sqlite-whentouse#Q1            1
C9    sqlite-wal#Q1                  1
C10   sqlite-wal#Q2                  1
C11   sqlite-wal#Q3                  1
C12   duckdb-sigmod19#Q3             1
C13   duckdb-cidr20#Q1               1
C14   sqlite-cli#Q1                  1
C15   sqlite-cli#Q2                  1
C16   sqlite-whentouse#Q3            1
C17   duckdb-cidr20#Q2               1
C18   duckdb-cidr20#Q2               1
C19   duckdb-cidr20#Q3               1
C20   duckdb-sigmod19#Q1             1
C21   sqlite-wal#Q2                  1
C22   sqlite-changes#Q2              1
C23   duckdb-sigmod19#Q3             1
C24   sqlite-lts#Q1                  1
C25   sqlite-lts#Q2                  1
C26   sqlite-lts#Q3                  1
C27   sqlite-changes#Q1              1
C28   sqlite-changes#Q3              1
C29   duckdb-pvldb-hashjoin#Q2       1

The same loop, printing only rows whose count is not 1 (must be empty):
$ grep -E "\| OK \|$" claims/claim-map.md | awk -F'|' '{gsub(/^ +| +$/,"",$2);gsub(/^ +| +$/,"",$6);gsub(/^ +| +$/,"",$7);gsub(/`/,"",$6);print $2"\t"$6"\t"$7}' | while IFS=$'\t' read -r id kq frag; do k=${kq%%#*}; q=${kq##*#}; qt=$(grep -A1 "^### $q " "sources/$k.md" | sed -n '2s/^> //p'); c=$(printf '%s' "$qt" | grep -F -c "$frag"); [ "$c" = 1 ] || printf '%s %s %s\n' "$id" "$kq" "$c"; done

==============================================================================
## §7 - the captures checked above are the ones level 1 fetched and logged (sha256)
==============================================================================

§1 greps quotes against sources/raw/<key>.txt. That only means anything if those files are
still the captures sources/CAPTURE_LOG.md recorded at level 1. Every hash below must appear
in the log. (The `.headers.txt` files are deliberately not in this check: level 1 logged a
hash for each `.src` and each `.txt`, not for the header dumps.)
$ shasum -a 256 sources/raw/*.src $(ls sources/raw/*.txt | grep -v headers)
51521141d0986bf823727598651562f2737146a666e4dcbb069455067bba9ed4  sources/raw/duckdb-cidr20.src
5df6abe28d57703a8792c4e91aaef321d46f8a2de4531c629d75a8273318eb5e  sources/raw/duckdb-pvldb-hashjoin.src
43fd71e165449dfce26f3e15b7c6452ac6c7f3bbedfaac41fd30949bb4bbbd9a  sources/raw/duckdb-sigmod19.src
7affb06012865c2b6026630fb6e9c2307642d5930a91857068184a17a0740f41  sources/raw/sqlite-changes.src
3d0e7bf59cab38cc4609e8dd4de51c947bc9a643d45e1d4413b65713d8688c42  sources/raw/sqlite-cli.src
1c8bc5dfbfca0f3cf7317cef15663ac0a9a2022770d0f06e0c05643d38108361  sources/raw/sqlite-lts.src
29315aaa998c5e2acf29e3ac161e9e8bf786d0c82a85ac83eb4f4091234acb7b  sources/raw/sqlite-wal.src
d0850096c26e9c888acaf2d6e2ac029e86f66f51a87461b77b94f1639d309b79  sources/raw/sqlite-whentouse.src
b48ccc47b42900033ddb857ae26a9b810261f8724d85a92817881b51e894c994  sources/raw/duckdb-cidr20.txt
8edaeacdb0bfc142995e45a20ba7e6fc232ac3b436f62830a72bc6a954fc4ceb  sources/raw/duckdb-pvldb-hashjoin.txt
1fd8fdcf36e85b2f69808b5adc1beeb7e031b800a0e04926ec9e298efede0917  sources/raw/duckdb-sigmod19.txt
c620bae85a2e9a24e3b362584fa405fc47af54516b54fcadd989039682900fc6  sources/raw/sqlite-changes.txt
e2d18f7a44989e4701db7f1bd6efb9d44246461b39c592344bef645038987aa6  sources/raw/sqlite-cli.txt
11472f71ffff6dca36dc6fb10708f914e43a1b6417d24fc239788ce2ffdb9e2a  sources/raw/sqlite-lts.txt
344d748072ca5e4df3e46656944b116c1de75d99a507e44f5573b953f59e9980  sources/raw/sqlite-wal.txt
d81424b957a9a05c6b457d6c76233734d356f7b0f96a49c619aa650dc400d63b  sources/raw/sqlite-whentouse.txt

Each of those hashes looked up in sources/CAPTURE_LOG.md (a 0 would mean a capture was
edited or replaced after level 1 logged it):
$ shasum -a 256 sources/raw/*.src $(ls sources/raw/*.txt | grep -v headers) | while read h f; do printf '%-45s %s\n' "$f" "$(grep -c "$h" sources/CAPTURE_LOG.md)"; done
sources/raw/duckdb-cidr20.src                 1
sources/raw/duckdb-pvldb-hashjoin.src         1
sources/raw/duckdb-sigmod19.src               1
sources/raw/sqlite-changes.src                1
sources/raw/sqlite-cli.src                    1
sources/raw/sqlite-lts.src                    1
sources/raw/sqlite-wal.src                    1
sources/raw/sqlite-whentouse.src              1
sources/raw/duckdb-cidr20.txt                 1
sources/raw/duckdb-pvldb-hashjoin.txt         1
sources/raw/duckdb-sigmod19.txt               1
sources/raw/sqlite-changes.txt                1
sources/raw/sqlite-cli.txt                    1
sources/raw/sqlite-lts.txt                    1
sources/raw/sqlite-wal.txt                    1
sources/raw/sqlite-whentouse.txt              1

==============================================================================

## End of transcript. Generated 2026-08-17 23:29 UTC by evidence/level-2/make-map-check.sh,
## repository root /tmp/aiceo-smoke.

==============================================================================
